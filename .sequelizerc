const path = require('path');

module.exports = {
  config: path.resolve(__dirname, 'src/config/config.js'), // Point to the TypeScript config file
  modelsPath: path.resolve(__dirname, 'src/models'),        // Models path
  migrationsPath: path.resolve(__dirname, 'src/migrations'), // Migrations path
  seedersPath: path.resolve(__dirname, 'src/seeders')      // Seeders path
};
